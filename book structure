//WAP to create a Book structure with its properties and create 3 instances and print their values
struct Book {
    let name:String
    let year:String
    let pages:String
    }
    let book = Book(name:"Swift",year:"2000",pages:"650")
    print(book.name)
    print(book.year)
    print(book.pages)
